# 🌌 AAT Phoenix Ascendant (Hardened)
**Version**: V3.1.4 | **Build**: Institutional Core

To launch the system:
1. Configure `config/settings.json`.
2. Compile/Attach MQL5 Gateway from `gateways/mql5/`.
3. Run `START_AAT.bat`.

Features: Ultra-low latency IPC, Kanban HUD, Manual Command Center, Dynamic Parameter Tuning.
